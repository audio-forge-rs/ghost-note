# Supabase Configuration
# https://supabase.com/docs/guides/cli/config

[project]
id = "ghost-note"

[api]
# Port for local development
port = 54321
# CORS configuration
extra_cors_headers = ["content-type"]

[functions]
# Default verify JWT for functions
# Set to false since we're using public API
verify_jwt = false
